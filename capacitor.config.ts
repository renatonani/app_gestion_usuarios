import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.nani2.qr',
  appName: 'app-7',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
